# Phase 1 Data Model: Invalid-Guess Shake Feedback

**N/A** — this feature introduces no data entities; it adds a transient animation
(`_shakeController`) triggered by the core play loop's (item 7) existing rejection
decisions, with no state that outlives the animation itself.
