# Phase 1 Data Model: Physical Keyboard Support

**N/A** — this feature introduces no data entities; it maps physical key events onto the
existing round-state methods (`_addLetter`, `_removeLetter`, `_submitGuess`) defined by
the core play loop (item 7).
