# Phase 1 Data Model: New Game Flow

**N/A** — this feature resets existing round-state fields owned by the core play loop
(item 7), the answer-reveal helper (item 17), and the win celebration (item 12); it
introduces no new entities of its own.
